# Assumptions

<!-- TODO: list assumptions made during context building -->

Each assumption should be a single line, marked with `[ASSUMPTION]`:

- [ASSUMPTION] The project uses Python 3.11+ as its runtime.
- [ASSUMPTION] The database is PostgreSQL 15+.

Questions that need verification go here too:

- [QUESTION] Does the project need to support Windows?
- [QUESTION] What's the expected deployment target?
