# Info

## Policy — who's authorized for each gate

Full gate list and explanations live in `LAAW/workflow.md §5`
— this section holds only the values, not the reasoning, so keep it
short.

```yaml
mode: assisted   # manual | assisted | autonomous

overrides:
  # Only needed for exceptions to your mode's default (see
  # LAAW/workflow.md §5 for what each mode defaults to).
```
